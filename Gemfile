source "https://rubygems.org"

# Use the GitHub Pages gem set for Jekyll + plugins
gem "github-pages", group: :jekyll_plugins

# Add last-modified timestamps sourced from git commits
gem "jekyll-last-modified-at"
